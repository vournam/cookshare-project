{{> regNavbar}}

<div class="container px-5 py-5">
    <div class="row">
        <h1 style="font-size:30px;"><i class="fa-solid fa-magnifying-glass"></i> Οι συνταγές μου</h1>
        <hr>
         {{#each data}}
            <div class="col-md-3 mb-3">
                <div class="card">
                    <img class="img-fluid" alt="" src={{this.img}}>
                    <div class="card-body">
                        <h4 class="card-title">{{this.title}}</h4>
                        <p class="card-text">{{this.time}}{{this.portions}}{{this.level}}</p>
                        <a class="all-buttons btn btn-secondary mb-1 mr-1 d-flex justify-content-center" href="/nonRegRecipe" role="button">Δες αναλυτικά</i></a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>

{{> footer}}